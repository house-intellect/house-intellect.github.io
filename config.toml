BaseURL="https://house-intellect.github.io/"
CanonifyURLs=true
languageCode = "en-us"
defaultContentLanguage = "en"
enableRobotsTXT = true
title = "Productivity Tracking"
theme = "hermit"
# enableGitInfo = true
pygmentsCodefences  = true
pygmentsUseClasses  = true
# hasCJKLanguage = true  # If Chinese/Japanese/Korean is your main content language, enable this to make wordCount works right.
rssLimit = 10  # Maximum number of items in the RSS feed.
copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License." # This message is only used by the RSS template.
enableEmoji = true  # Shorthand emojis in content files - https://gohugo.io/functions/emojify/
googleAnalytics = "G-RBE05T0Q3R"
# disqusShortname = "yourdiscussshortname"
#  hugo && firebase deploy

[author]
  name = "house-intellect"
  

[blackfriday]
  # hrefTargetBlank = true
  # noreferrerLinks = true
  # nofollowLinks = true

[taxonomies]
  tag = "tags"
  # Categories are disabled by default.

[params]
  dateform        = "Jan 2, 2006"
  dateformShort   = "Jan 2"
  dateformNum     = "2006-01-02"
  dateformNumTime = "2006-01-02 15:04 -0700"

  # Metadata mostly used in document's head
  description = "NFC Progress Report Android admin app assigns dynamic URLs to read-only NFC tags and distributes mapping metadata in a shared Google Calendar named NFC for the client NFC Task List Android apps to open relevant dynamic links upon scanning read-only NFC tags"
  images = ["scan.png"]
  themeColor = "#494f5c"

  homeSubtitle = "using URLs in Google Calendar events assigned to NFC tags"
  footerCopyright = ''
  # bgImg = ""  # Homepage background-image URL

  # Prefix of link to the git commit detail page. GitInfo must be enabled.
  # gitUrl = "https://github.com/username/repository/commit/"

  # Toggling this option needs to rebuild SCSS, requires Hugo extended version
  justifyContent = false  # Set "text-align: justify" to `.content`.

  relatedPosts = false  # Add a related content section to all single posts page

  code_copy_button = true # Turn on/off the code-copy-button for code-fields

  # Add custom css
   customCSS = ["photo.css"]

   tags = ["NFC", "progress report", "progress monitoring", "automated progress monitoring", "automatic progress report", "task list", "check list", "maintenance manuals", "servicing instructions", "productivity tracking"]

[menu]

  [[menu.main]]
    name = "NFC task list"
    url = "/posts/nfc_checklist/"
    weight = 0

  [[menu.main]]
    name = "NFC progress report"
    url = "/posts/nfc_reports/"
    weight = 0
    
[[menu.main]]
    name = "Privacy Policy"
    url = "/posts/privacy_policy/"
    weight = 0

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true